model MasterService {
  id              String                @id @default(uuid())
  name            String                @map("name")
  description     String                @map("description")
  price           Float                 @map("price")
  rating          Float                 @default(0) @map("rating")
  durationMinutes Int                   @default(60) @map("duration_minutes")
  category        MasterServiceCategory @default(SERVICES) @map("category")
  tags            String[]              @default([]) @map("tags")

  masterProfileId String @map("master_profile_id")

  masterProfile MasterProfile           @relation(fields: [masterProfileId], references: [id])
  appointments  Appointment[]
  reviews       MasterServiceReview[]
  favorites     FavoriteMasterService[]
  interactions  UserServiceInteraction[]

  createdAt DateTime  @default(now()) @map("created_at")
  updatedAt DateTime  @updatedAt @map("updated_at")
  deletedAt DateTime? @map("deleted_at")

  @@index([category])
  @@index([rating])
  @@index([masterProfileId, deletedAt])
  @@index([deletedAt, category, rating])
  @@index([deletedAt, category, price])
  @@map("MasterServices")
}

enum MasterServiceCategory {
  BEAUTY
  REPAIR
  AUTO
  HOME
  PARTS
  SERVICES
  DIGITAL
  PHOTO
  SPORT
  DESIGN
  WELLNESS
}
