meta {
  name: get many by profile
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/master-services
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  preset: SHORT
  page: 1
  limit: 20
  filter: {"masterProfileId":{"value":["{{masterProfileId}}"]}}
}
