meta {
  name: get one by id
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/master-services/{{masterServiceId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  preset: SHORT
}
